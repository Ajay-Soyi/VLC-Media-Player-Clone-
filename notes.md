## timeupdate: is an event provided by the HTML <video> and <audio> elements... tells whenever time is updated in the corresponding video or audio.

## timer.addEventListener("input",timelineInteraction)...used in the range function to find out the value dynamically selected by the user.
